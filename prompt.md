# 🎯 科研团队管理多智能体系统开发任务

## 角色定位
你是一位精通多智能体系统（Multi-Agent System）架构的 Python 专家，需基于现代 Agent 框架（如 LangChain/LangGraph）构建一套**科研团队全生命周期管理智能体系统**。

---

## 📋 核心功能需求（按优先级排序）

### P0 - 核心数据管理
1. **成员注册 Agent**  
   - 维护成员画像：姓名、学号、邮箱、研究方向（多标签）、个人网站/GitHub 等元数据
   - 支持 CRUD 操作与数据持久化（SQLite/JSON）

2. **项目追踪 Agent**  
   - 记录项目全生命周期：历史归档项目 + 进行中项目
   - 追踪里程碑进度，当检测到进度停滞（如 7 天无更新）自动触发提醒机制

### P1 - 情报采集与分发
3. **信息抓取 Agent**  
   - 每日定时抓取：AI 领域热点新闻（如 TechCrunch、机器之心、Paper Digest）
   - 基于成员研究方向个性化推送：arXiv 最新论文（按关键词/作者订阅）

### P2 - 运维与自愈
4. **日报生成 Agent**  
   - 每日 24:00 自动汇总：当日交互日志、系统运行指标、异常事件
   - 输出结构化运行状态报告（Markdown/JSON）

5. **监管与自愈 Agent**（独立进程）  
   - 监控主任务 Agent 健康状态，捕获异常后执行分级修复策略：
     - Level 1: 自动重启/重试
     - Level 2: 回滚到上一稳定状态
     - Level 3: 生成故障报告并人工介入
   - 维护修复审计日志

---

## 🏗️ 技术实现规范

### 架构要求
- **模块化设计**：每个 Agent 为独立微服务，通过 API 通信
- **扩展接口**：预留以下接入点供后续迭代：
  - `Memory 模块`: 支持短期对话缓存 + 长期向量记忆（Chroma/FAISS）
  - `MCP 协议`: 实现 Model Context Protocol 服务端/客户端
  - `Skills 注册中心`: 动态加载工具函数（如邮件发送、日历同步）
  - `RAG 管道`: 论文/文档的嵌入、检索、重排序接口

### 交付物清单
1. **项目看板** (`TODO.md`)  
   - 采用 MoSCoW 优先级法拆解任务
   - 每项含：预计工时、依赖项、完成状态 [ ] / [x] + 时间戳

2. **防御性设计**  
   - 所有任务需返回 `TaskResult` 对象： `{status: success|failed|partial, data: any, error_msg: str|null}`
   - 不可完成任务明确标记为 `[BLOCKED]` 并说明原因

3. **技术文档** (`README.md`)  
   - 系统架构图（Mermaid 流程图）
   - Agent 交互时序图
   - 快速启动指南 + 环境变量配置模板

---

## ⚡ 启动指令
"请基于以上需求，首先输出完整的项目架构设计与 TODO.md 初稿，确认后再进入编码阶段。"